{
    "ReportName" : "DailyFLASHiZReport",
    
    "ReportHeaderText" : "Daily FLASHiZ Report",
                            
    "ReportHeader" : [   
                           [ "Date Range From" , "${StartTime}", "To" , "${EndTime}" ]
                     ],
    
    "ReportHeaderDataColumnsWidth" : [ 5,3,1,8	],
         
    "HeaderColumns" : [ "DATE.TIME",  
    					"AMOUNT TXN", 				
    					"RRN", 
    					"MESSAGE"
    					],
    
    "HeaderColumnsWidth" : [1,1,1,1],

    "EncryptedColumnIndices" : [],
        
    "Query" : " SELECT
				    DATE_FORMAT(sctl.CreateTime, ${DateTimeFormat}),
					Format(sctl.TransactionAmount, 2),
					concat('BPAY-', LPAD(sctl.id, 12, '0')),
					LPAD(sctl.InvoiceNo, 12, '0')
				FROM
					commodity_transfer ct
					INNER JOIN service_charge_txn_log sctl ON ct.ID = sctl.CommodityTransferID
					INNER JOIN transaction_type tt ON tt.ID= sctl.TransactionTypeID
				WHERE
					tt.TransactionName in ('QRPayment')
					AND sctl.CreateTime >= ${StartTime}
					AND sctl.CreateTime < ${EndTime}
				ORDER BY
				     sctl.CreateTime ASC",
    
    "ReportFooter" : [ 
                           [ "Report Generation Date" , "${ReportGenerationDate}", "Generated By" , "${UserName}" ]
                          ],
    
    "ReportFooterDataColumnsWidth" : [ 4,3,3,3],
    
    "DateTimeFormatOfDataFromDB" : "%y%m%d%H%i",
    
    "ReportType" : ["pdf", "xls", "txt"],
    
    "PageSize" : "A4",

    "PageOrientation" : "portrait"
    
}